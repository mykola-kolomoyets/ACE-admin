/* eslint-disable no-unused-vars */
export enum ButtonView {
  primary = 'primary',
  ghost = 'ghost',
  link = 'link',
  red = 'red',
  transparent = 'transparent',
  fullRed = 'fullRed'
}

export enum ExportOption {
  dashboard = 'dashboard',
  users = 'users'
}
