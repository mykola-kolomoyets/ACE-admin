/* eslint-disable no-unused-vars */
/**
 * Enum to define the status of voucher
 * @enum {String}
 * @readonly
 */
export enum VoucherStatus {
  VALID = 'VALID',
  REDEEMED = 'REDEEMED',
  EXPIRED = 'EXPIRED'
}
