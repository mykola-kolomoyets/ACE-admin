/* eslint-disable no-unused-vars */
/**
 * Enum to define the status of user
 * @enum {String}
 * @readonly
 */
export enum UserStatus {
  active = 'Active',
  inactive = 'Inactive',
  pending = 'Pending'
}
